package com.grepp.llm;

/**
 *   Generation Format을 지정하기 위한 인터페이스 입니다.
 *   PropertyFormat 타입을 사용해 LLM으로 부터 응답받기를 희망하는 형식을 지정할 수 있습니다.
 *
 *   ex)
 *   public record Lang(PropertyType name, PropertyType level, PropertyType reason)
 */
public interface Format {

}
